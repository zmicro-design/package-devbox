#!/bin/bash

help() {
  echo "Usage:"
  echo "  zmicro devbox <args ...>"
  echo ""

  devbox --help
}

version() {
  devbox --version
}

core() {
  # context directory
  local context="$ZMICRO_PACKAGES_DATA_PATH/devbox"
  if [ ! -d "$context" ]; then
    mkdir -p $context
  fi
  cd $context
  # context directory

  case $1 in
  -h | --help | help)
    help
    ;;
  -v | --version | version)
    version
    ;;
  *)
    devbox "$@"
    ;;
  esac
}

run() {
  core "$@"
}

run "$@"
